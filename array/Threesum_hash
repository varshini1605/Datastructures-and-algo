def threesum(arr):
    n=len(arr)
    hashset=set(arr)
    for i in range(n-2):
        for j in range(1,n-1):
            if (i+j)* -1 in hashset:
                return True
            
    return false
        
if __name__=="__main__":
    arr = [0, -1, 2, -3, 1]
    print(threesum(arr))
